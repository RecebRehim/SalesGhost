# Optional: cross-device sync + n8n visibility via Supabase (free tier).
# Create project at https://supabase.com — run supabase-schema.sql — paste URL + anon key.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# REST API for n8n is configured on the server (Vercel), not in this file — see ../env.rest.example
